%select{name: "q[category_id_in]", id: "select_category_children", class: "js"}
  %option{value: @parent_id} 全て
  - @children.each do |child|
    %option{value: child.id, data:{size:{id: child.size_id}}}= child.name
