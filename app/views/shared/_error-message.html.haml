- if model.errors.full_messages_for(column.to_sym).any?
  - unless  model.valid?
    - if errors = model.errors.full_messages_for(column.to_sym)
      %ul
        - errors.each do |error|
          %li= error
